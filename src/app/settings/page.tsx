import Layout from "@/components/dashboard-shell";

export default function SettingsPage() {
  return (
    <Layout>
      <h1 className="text-2xl font-bold mb-4">Settings</h1>
      <p>Account, preferences, integrations, etc.</p>
    </Layout>
  );
}
